import * as path from "path-browserify";


const { 
  resolve, 
  join, 
  dirname, 
  basename, 
  extname, 
  format, 
  parse, 
  sep, 
  delimiter 
} = path;

export { 
  // Path methods
  resolve, 
  join, 
  dirname, 
  basename, 
  extname, 
  format, 
  parse, 
  sep, 
  delimiter
};

 
