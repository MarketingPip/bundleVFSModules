import {
  setTimeout,
  setImmediate,
  setInterval,
  scheduler,
} from 'node:timers/promises';

export * from 'node:timers/promises';

export default {
  setTimeout,
  setImmediate,
  setInterval,
  scheduler,
};
