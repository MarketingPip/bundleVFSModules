// node:_stream_transform — Node internal module (module.exports = Transform).
// Thin re-export of the stream port's Transform class.
// Dependency-free ESM, browser-safe. No _RUNTIME_ access needed.
import { Transform } from './stream.js';
export default Transform;
